# GitHub Release template

Use this text as the body of a GitHub Release.

## Release summary

`whmcs-blueonyx-server-module` version `<version>`

## Highlights

- BlueOnyx server module for WHMCS
- BlueOnyx login handoff and customer-facing service actions
- Self-contained packaging for repository publication

## Changes

See the matching entry in [`CHANGELOG.md`](../CHANGELOG.md).

## Installation

This release is intended to be installed by copying the `modules/servers/blueonyx/`
directory into a matching WHMCS installation or by deploying the repository
contents as a package.

## Notes

- Git tag format: `v<version>`
- Release asset: `whmcs-blueonyx-server-module-<version>.zip`
- The repository is maintained independently from the BlueOnyx core project
